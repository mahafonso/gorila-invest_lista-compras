import { Injectable } from '@angular/core';
import { ListComponent } from './list.component';

@Injectable()
export class ListService {
  // products: ListComponent = new ListComponent();

  listProducts() {
    // this.products.listProducts();
  }
}
